<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blaumtask">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_heart_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.LoginActivity"></activity>
        <activity android:name=".ui.SignupActivity" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.FinishOrderActivity" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity android:name=".ui.CheckoutActivity" />
        <activity android:name=".ui.My_Orders" />
        <activity android:name=".ui.ProductsDetailsActivity" />
        <activity android:name=".ui.SearchActivity" />
        <activity android:name=".ui.CartSettingsActivity" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>